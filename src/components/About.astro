<section id="about" class="about">
  <div class="section-content">
    <h3 class="section-title"><span class="title-accent emoji-replace">about</span> <span class="mobile-hide">me</span></h3>
    <p class="scroll-reveal-effect mobile-hide">Greetings! I blur the lines of creativity and tech to produce captivating and interactive websites. My background in hospitality prepares me for fast-paced and demanding work environments, where I excel at juggling tasks and collaborating with team members to bridge the client-consumer divide. On my days off, you can find me exploring new coffee joints in Logan Square or taking a scenic bike ride along the 606 trail.</p>
    <p class="mobile-show">Greetings! I blur the lines of creativity and tech to produce captivating and interactive websites. My background in hospitality prepares me for fast-paced and demanding work environments, where I excel at juggling tasks and collaborating with team members to bridge the client-consumer divide. On my days off, you can find me exploring new coffee joints in Logan Square or taking a scenic bike ride along the 606 trail.</p>

  </div>
</section>

<style>
  .about {
  position: relative; 
  display: block;
  margin: 0 auto;


}

  .section-content {
    margin: 0 auto;
    overflow-x: hidden;
  }

  .section-title {
    font-size: 3.5rem;
    color: var(--grey);
  }

  .section-content p {
    width: 80rem;
    max-width: 100%;
    margin: 0 auto;
    font-size: 2.5rem;
    color: var(--body-text);
    font-family: var(--neue-text);
    line-height: 1.7;
    white-space: pre-wrap;
  }

  .title-accent {
    color: var(--primary-text);
    font-weight: 700;
    font-size: 6rem;
  }

.about span.letter {
  font-family: var(--neue-text);
  font-size: 2.5rem;
  color: var(--body-text);
  opacity: 0.1;
  transition: opacity 1s linear;
}

@media only screen and (max-width: 761px) {

.title-accent {
  font-size: 4rem;
}

.section-content p {
  font-size: 1.8rem;
  padding-top: 2rem;
  line-height: 1.7;
}
</style>
<script>
document.addEventListener("DOMContentLoaded", () => {
  const textWrapper = document.querySelector(".scroll-reveal-effect");

  if (textWrapper) {
    textWrapper.innerHTML = textWrapper.textContent.replace(/\S/g, "<span class='letter'>$&</span>");

    const letters = document.querySelectorAll("#about .letter");

    const adjustOpacity = () => {
      const factor = 3.7; // Controls the speed of the scroll effect
      const offset = 300; // Controls when the scroll effect starts
      const scrollProgress = factor * (document.documentElement.scrollTop - offset);
      const scrolled = scrollProgress / (document.documentElement.scrollHeight - document.documentElement.clientHeight) * letters.length;
      const fadeRange = 50; // Number of letters over which the opacity will transition

      letters.forEach((letter, index) => {
        const distance = scrolled - index;
        let opacity;

        if (distance > fadeRange) {
          opacity = 1;
        } else if (distance < 0) {
          opacity = .1;
        } else {
          opacity = Math.max(0.1, distance / fadeRange);
        }

        (letter as HTMLElement).style.opacity = opacity.toString();
      });
    };
    adjustOpacity();
    document.addEventListener("scroll", adjustOpacity);
  }
});

</script>
